# 執行步驟規劃（移植路線圖）

## 移植風格（總原則）
**以 C 風格逐函式翻譯為主，盡量只做語法轉換，不重新設計架構。**
- 每個 `.c` → 一個同名 `static class`；C 全域 → `static` 欄位；C 函式 → `static` 方法。
- **保留原始識別字命名**（如 `mt_srand`、`JE_loadPic`、`JE_byte`），方便與 `sources/` 逐行對照。
- **盡量用非託管陣列/指標**（`unsafe`、`NativeMemory`）對應 C 的 `static` 陣列與指標運算；
  全域緩衝在啟動時配置一次、結束時統一釋放，**嚴禁 memory leak**（見 `MD/Reference/移植風格與C慣例.md`）。
- 減少不必要的物件包裝；把整個 GAME LOOP 當成 C 程式去完成。
- Ports 介面（`IVideoBackend` 等）僅作為 SDL 邊界薄接縫；遊戲邏輯不直接碰 SDL。

## 階段與步驟（依原始相依順序）

### Phase A — 基礎設施
1. [x] 移植風格與 C 慣例文件 + `CMem`（malloc/calloc/free/realloc 包裝，含 leak 追蹤）
2. [x] `Opentyr`：常數/巨集（COUNTOF/MIN/MAX/M_PI、TYRIAN_VERSION）、`JE_*` 型別別名、版本字串
3. [x] `MtRand`：移植 `mtrand.c`（指標式 Mersenne Twister，序列須與原版一致）
4. [x] `Globals`：static 平台參考（Video/Audio/Input/Clock/Files）+ 資料/使用者目錄；
       `TyrianHaltException` 取代 exit()；`Varz.JE_tyrianHalt`（暫行）

### Phase B — 檔案與資料 I/O
5. [x] `CFile`：`file.c`（data_dir、dir_fopen、fread/fwrite_die、typed 讀寫；FILE*→Stream，LE）
6. [x] `MemIO`：`memreader.c`/`memwriter.c`（MemReader/MemWriter 指標式 LE 讀寫）
7. [x] `Config`：`config.c`（設定/存檔，little-endian，用已移植的 MemIO）
   - [x] 二進位設定 tyrian.cfg、存檔/高分 tyrian.sav（含加解密、預設高分榜）、JE_saveGame/JE_loadGame
   - [x] JE_initProcessorType/JE_setNewGameSpeed、playeritems<->pitems、JE_SaveFileType + 全域
   - [ ] `config_file.c`(opentyrian.cfg INI) 與 episode 資料載入：暫最小占位

### Phase C — 影像與調色盤
8. [x] `Palette`：`palette.c`（JE_loadPals、set_palette/colors、fade_palette/solid/black/white；
       SDL_MapRGB→自有 MapRGB；變更即上傳 IVideoBackend）。yuv_palette(hqNx) 暫略。
9. [x] `Video` / `Vga256d`：`video.c`/`vga256d.c`
   - [x] `SdlShim`(SDL_Color/Rect/Surface 自有相容型別) + `Vga256d`(繪圖原語) + `Sdl.SDL_FillRect`
   - [x] `video.c`：VGAScreen 等 surface、init_video/JE_clr256/JE_showVGA/deinit_video → 接 `IVideoBackend`
   - [x] 註：視窗/縮放/全螢幕由 App SdlVideo adapter 負責；mapScreen/Window 座標待 mouse 移植時補
   - [~] 計時/輸入最小橋接：`Nortsong`(frameCount 計時)、`Keyboard`/`Mouse`(waitUntilElapsed/clearInput 暫行)；
     完整 `keyboard.c` 待遊戲邏輯需要時以**中性事件佇列**移植（保持 Core SDL-free）

> **可視里程碑**：ported video/palette/vga256d 管線實機跑通（VGAScreen 非託管 → JE_showVGA → IVideoBackend）。

10. [x] `Pcxload` / `Picload`(+`Pcxmast`)：`pcxload.c`/`picload.c`/`pcxmast.c`（RLE PCX/PIC 整頁圖載入）
    - 可載入真實 `tyrian.pic`/`tshp2.pcx` → VGAScreen，里程碑：實機輪播真實 Tyrian 圖片
11. [x] `Sprites` / `FontDraw` / `Fonthand`(+`Sndmast`)：`sprite.c`/`font.c`/`fonthand.c`/`sndmast.h`
    - Sprite/Sprite2 結構 + 全 blit 變體（透明/混色/變色/變暗/clip/2x2）、shape table 載入
    - fontMap、JE_textWidth/outText/dString/textShade/outTextAdjust… + FontDraw 對齊/陰影
    - 里程碑：真實 tyrian.shp 字型疊在真實 tyrian.pic 圖上顯示文字
12. [~] `Backgrnd` / `Starlib`：`backgrnd.c`/`starlib.c`
    - [x] `Starlib`(starlib.c)：3D 星空模擬/繪製（互動熱鍵暫簡化為 ESC；自動切換 setup 保留）→ 標題星空背景
    - [x] `backgrnd.c`：三層捲動背景繪製(blit_background_row/blend、draw_background_1/2/3、JE_darkenBackground)
          flat-index 走訪 megaData.mainmap；實機驗證 draw_background_1=56448/64000 非零像素

### Phase D — 音訊
13. [x] `Opl`：`opl.c`（DOSBox OPL2 FM 模擬器，9 通道，捨棄 OPL3；指標→陣列索引/ref）
14. [x] `Loudness`：`loudness.c`（混音）→ 接 `IAudioBackend`（callback=IAudioSource，Lock/Unlock 保護）
15. [~] `Lds`(lds_play.c 完整音樂播放器) / `Nortsong`(計時+loadSndFile+JE_playSampleNum+JE_changeVolume) / `Sndmast`
    - **OPL FM 音樂 + 音效(SFX) 皆可發聲**
    - `MusMast` / `Jukebox` 待

### Phase E — 輸入
16. [x] `Keyboard`(完整 keyboard.c) / `Mouse`(mouse.c) / `Joystick`(stub)：
    - **中性事件佇列**：`IInputBackend.PollEvent` 回傳中性 `PlatformEvent`（App SdlInput 翻譯 SDL_Event），
      Core 保持 SDL-free；`SdlKeys` 提供 SDL scancode/keymod/KEY_COMBO 常數
    - keysactive/鍵盤+滑鼠佇列/焦點/文字輸入(CP437)/lordKeys/wait* 全移植
    - `IVideoBackend` 加 MapWindowToScreen/ToggleFullscreen；移除舊 GameKey 抽象

### Phase F — 遊戲流程、選單、主迴圈

16. [x] **資料結構**（`Varz` / `MegaData` / `Player` / `Episodes` / `Lvlmast` / `Config`）
    - [x] `Varz`：varz.h 常數/enum/資料結構（JE_SingleEnemyType / EnemyShotType / Explosion /
      rep_explosion_type / superpixel_type / JE_EventRecType + enemy / enemyShot / explosions… 全域陣列）
    - [x] `Varz` 函式與 const 資料表（explosions / superpixels / HUD bars / portConfigs / drawOptions 自足子集）
    - [x] `MegaData` 地圖結構（`JE_MegaData`：mainmap=nint[] / shapes=非託管 672-byte blob + megaData1/2/3 + alloc/free）
    - [x] `Lvlmast`(維度常數) + `Player`(PlayerItems / Player / sidekick + player[2]) +
      `Episodes`(物品/敵人結構 + 全域) + `Config`(最小)

17. [~] **資料載入與輔助**（`Episodes` / `Lvllib` / `Helptext` / `ArgParse` / `Params` / `Nortvars` / `Animlib`）
    - [x] `Episodes.JE_loadItemDat`(tyrian.hdt→武器/port/special/power/ship/option/shield/enemyDat) +
      JE_initEpisode / scanForEpisodes / findNextEpisode
    - [x] `Lvllib.JE_analyzeLevel`(讀 tyrian{N}.lvl 取關卡偏移)
    - [x] `Helptext.JE_loadHelpText`(tyrian.hdt 加密 Pascal 字串→選單/說明/船艦資訊文字) + JE_helpBox / HBox
    - [x] `ArgParse`(getopt 重實作) + `Params.JE_paramCheck`(網路選項視為不支援) + `Xmas`(占位)
    - [x] `Nortvars`(JE_dBar3 / JE_barDrawShadow) + `Varz.JE_drawShield` / `JE_drawArmor`
    - [x] `Animlib.playAnim`(.ANM 動畫播放)
    - [ ] varz 重度函式(specialComplete / doSpecialShot，需 button[] / levelFilter)

18. [ ] `Menus` / `GameMenu` / `EditShip` / `Destruct`

19. [~] **玩家與射擊**（`Player` / `Shots`）
    - [x] `Shots`：player_shot_create / move_and_draw / set_direction / simulate_player_shots
    - [ ] `player.c`（玩家移動/碰撞，待主迴圈）

20. [~] **主迴圈**（`MainInt` / `Tyrian2`）—— 逐步切割移植中
    - [x] `Mainint.JE_initPlayerData` / `JE_drawPortConfigButtons`
    - [x] `Tyrian2.intro_logos`（開場 logo 動畫，圖 10/12 + 淡入；設 moveTyrianLogoUp→標題 logo 升起）
    - [x] **`Tyrian2.titleScreen`（真實標題畫面/主選單）**：tyrian.pic 標題圖 + logo + 選單導航(鍵盤/滑鼠)
      + SONG_TITLE 配樂 + 淡入 + 特殊碼偵測；Quit/ESC 結束
    - [x] **New Game 選單流程**：`Menus.gameplaySelect` / `episodeSelect` / `difficultySelect`(menus.c)
      + `Tyrian2.newGame`(玩家設定/cash/ship)
    - [x] `Mainint.JE_highScoreScreen`（高分榜，章節翻頁，接 titleScreen「High Scores」）
    - [x] `Mainint.JE_helpSystem` + `helpSystemPage`（說明系統，主題選單+分頁，接「Instructions」）
    - [x] `Mainint.JE_loadScreen` + `JE_operation`(load 路徑)（存檔載入畫面，接「Load Game」）
    - [x] **`Tyrian2.JE_loadMap`（關卡載入）**：episode 命令解析(過場/設定/跳關) + LEVELS.DAT / shapes?.dat→megaData
      （驗證 ep1lv1：maxEvent=1009 / levelEnemyMax=7 / mainmap 指標正確；itemScreen/nextEpisode/displayText 暫 stub）
    - [x] **`Tyrian2.JE_main` 骨架（進入關卡）**：忠實 setup(JE_loadMap + map 位置 + 玩家 + 背景初始化)；
      TyrianGame 標題迴圈接 JE_main（對應 opentyr.c main: initPlayerData→titleScreen→JE_main）
    - [x] 玩家控制（鍵盤移動 + 船艦繪製；完整 JE_playerMovement 待移植）
    - [x] 玩家射擊（button[0]→player_shot_create 前/後武器）
    - [x] 事件系統 `JE_eventSystem`（背景/星空/特殊武器事件分派 + eventLoc 前進）
    - [x] **`JE_makeEnemy` 敵人工廠**（由 enemyDat 初始化敵人；驗證 5 種敵人數值正確）
    - [x] **敵人生成**：`JE_createNewEventEnemy` / `JE_eventJump` + 事件 5(載入敵人圖)/6/7(生成)
      + 最小敵人移動繪製（驗證 ep1：50 敵人生成、4 圖庫載入）
    - [x] **子彈↔敵人碰撞**：player_shot_move_and_draw + box collision → armorleft 扣血 / 死亡爆炸 / 計分 / 回收
    - [x] **HUD**：`JE_inGameDisplays`(分數/特殊武器/超級炸彈) + 護盾/裝甲 bar
    - [ ] 完整 `JE_drawEnemy`(445 行 homing / 砲塔發敵彈 / 彈跳)、敵彈↔玩家碰撞、setupMenu(game_menu.c)

21. [ ] `Opentyr`：`opentyr.c` 進入點（`main` / `setupMenu`），串起整個 GAME LOOP

> 網路 `network.c`：**不移植**。

## 驗證節奏
- 每完成一步即 `dotnet build cs_ported/App/App.csproj -c Release -o Build`，保持 0 錯誤。
- 能上畫面的階段（Phase C 之後）以實機執行驗證。
- 進度同步更新 `cs_ported/PORTING.md` 與本檔。

## 版控節奏
- 版本庫：GitHub `erspicu/AprCSTyrian`（private，origin/main）。
- **階段性產出即 commit & push**：每完成一個（或一組）步驟、建置通過後就提交並推送。
- commit 訊息結尾附 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
