# 執行步驟規劃（移植路線圖）

## 移植風格（總原則）
**以 C 風格逐函式翻譯為主，盡量只做語法轉換，不重新設計架構。**
- 每個 `.c` → 一個同名 `static class`；C 全域 → `static` 欄位；C 函式 → `static` 方法。
- **保留原始識別字命名**（如 `mt_srand`、`JE_loadPic`、`JE_byte`），方便與 `sources/` 逐行對照。
- **盡量用非託管陣列/指標**（`unsafe`、`NativeMemory`）對應 C 的 `static` 陣列與指標運算；
  全域緩衝在啟動時配置一次、結束時統一釋放，**嚴禁 memory leak**（見 `MD/Reference/移植風格與C慣例.md`）。
- 減少不必要的物件包裝；把整個 GAME LOOP 當成 C 程式去完成。
- Ports 介面（`IVideoBackend` 等）僅作為 SDL 邊界薄接縫；遊戲邏輯不直接碰 SDL。

## 階段與步驟（依原始相依順序）

### Phase A — 基礎設施
1. [x] 移植風格與 C 慣例文件 + `CMem`（malloc/calloc/free/realloc 包裝，含 leak 追蹤）
2. [x] `Opentyr`：常數/巨集（COUNTOF/MIN/MAX/M_PI、TYRIAN_VERSION）、`JE_*` 型別別名、版本字串
3. [x] `MtRand`：移植 `mtrand.c`（指標式 Mersenne Twister，序列須與原版一致）
4. [ ] `Globals`：static 平台參考（Video/Audio/Input/Clock/Files）供 C 風格全域存取

### Phase B — 檔案與資料 I/O
5. [ ] `File`：`file.c`（路徑、JE_* 二進位讀寫 helper）
6. [ ] `MemReader`/`MemWriter`：`memreader.c`/`memwriter.c`
7. [ ] `ConfigFile` / `Config`：`config_file.c`/`config.c`（設定/存檔，little-endian）

### Phase C — 影像與調色盤
8. [ ] `Palette` / `VgaPalette`：`palette.c`/`vga_palette.c`
9. [ ] `Video` / `Vga256d`：`video.c`/`vga256d.c`（screen surface、blit）→ 接 `IVideoBackend`
10. [ ] `PcxLoad` / `PicLoad`：`pcxload.c`/`picload.c`
11. [ ] `Sprite` / `Font` / `FontHand`：`sprite.c`/`font.c`/`fonthand.c`
12. [ ] `Backgrnd` / `StarLib`：`backgrnd.c`/`starlib.c`

### Phase D — 音訊
13. [ ] `Opl`：`opl.c`（FM 合成）
14. [ ] `Loudness`：`loudness.c`（混音）→ 接 `IAudioBackend`
15. [ ] `LdsPlay` / `NortSong` / `SndMast` / `MusMast` / `Jukebox`

### Phase E — 輸入
16. [ ] `Keyboard` / `Joystick` / `Mouse`：`keyboard.c`/`joystick.c`/`mouse.c` → 接 `IInputBackend`

### Phase F — 遊戲流程、選單、主迴圈
17. [ ] `Params` / `ArgParse`、`Episodes`、`Varz`/`NortVars`、`HelpText`
18. [ ] `Menus` / `GameMenu` / `EditShip` / `Destruct`
19. [ ] `Player` / `Shots`
20. [ ] `MainInt` / `Tyrian2`（主迴圈/遊戲邏輯）
21. [ ] `Opentyr`：`opentyr.c` 進入點（`main`/`setupMenu`），串起整個 GAME LOOP

> 網路 `network.c`：**不移植**。

## 驗證節奏
- 每完成一步即 `dotnet build cs_ported/App/App.csproj -c Release -o Build`，保持 0 錯誤。
- 能上畫面的階段（Phase C 之後）以實機執行驗證。
- 進度同步更新 `cs_ported/PORTING.md`。
